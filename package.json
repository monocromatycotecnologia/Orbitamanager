{
  "name": "orbitamanager",
  "version": "0.1.0",
  "description": "Orbitamanager - A comprehensive project management solution",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "install-all": "npm install && npm install --workspace=frontend && npm install --workspace=backend"
  }
}
